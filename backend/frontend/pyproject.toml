[project]
name = "SaludNow"
version = "1.0.0"
description = "Aplicación Salud Now"
dependencies = [
    "requests",
    "SQLAlchemy",
    "pg8000",
    "python-jose",
    "urllib3"
]

[tool.flet]
permissions = ["biometric"]
app.module = "main"
